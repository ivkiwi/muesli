# Muesli / Гусли TODO

_Записано 2026-07-02. Пока не реализовывать._

- [ ] Добавить настройку папки, куда приложение складывает записи, когда включена запись встреч.
- [ ] Перевести сохраненные записи встреч с постоянного WAV на компактный аудио-only формат (`.m4a`/AAC): live pipeline оставить в PCM/WAV, после завершения встречи сжимать retained recording, а для ретранскрипции декодировать сохраненный файл во временный 16 kHz mono WAV и удалять temp-файл после ASR.
- [ ] Сделать пользовательский шаблон саммари, который уже обсуждали, шаблоном по умолчанию: один язык вывода по языку встречи, обязательное извлечение todo, исправлений, доделок и follow-up.
- [ ] Переименовать пользовательские надписи внутри приложения с «Мюсли» на «Гусли».
- [ ] Добавить миграцию истории из уже установленной Muesli в Гусли: найти старый `~/Library/Application Support/Muesli/muesli.db`, импортировать встречи, диктовки, транскрипты, заметки/summary и сохраненные аудиозаписи в новую историю приложения; Markdown сделать отдельной опцией batch export/import, а не единственным форматом переноса.
- [ ] Довести GigaAM-ретранскрипцию до того ручного пайплайна, который сработал на встрече 2026-07-02: для long-form и saved-recording retranscribe использовать 22s чанки с 20s шагом и 2s overlap, запускать одним warm batch там, где возможно, склеивать стыки с дедупом, не пропускать GigaAM через VAD-skip, показывать `Re-transcribe`/actions для failed и note-only встреч с сохраненной записью, сохранять статус `completed`, word count, fallback raw transcript и чистить все временные WAV/TSV/MD после завершения или ошибки.
- [ ] Добавить внешний анализатор/подчищатель транскрипта после ASR: через залогиненный ChatGPT или другой внешний провайдер вычищать overlap-дубли, чинить очевидные ASR-искажения доменных слов (`GitLab`, `ClickHouse`, `Claude`, `Chronos`, `Network`, `MR`, `Jira`), восстанавливать разрывы фраз на границах чанков, при наличии diarization/таймкодов проставлять стабильные `You`/`Speaker N`, возвращать очищенный raw transcript отдельно от summary и не терять исходный ASR-текст для отката/диагностики.
